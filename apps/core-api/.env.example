# Monno Platform Environment Configuration
# Copy this file to .env and update with your values

###################
# Core API Configuration
###################
NODE_ENV=development
PORT=4000
PROJECT_NAME=App
APP_VERSION=1.0.0

# Admin bootstrap (optional)
# If set, core-api will ensure a user with this phone exists and has the `admin` role on startup.
# Use E.164 format if possible (e.g. +989123456789).
ADMIN_PHONE=''

###################
# Database Configuration
###################
POSTGRES_USER='postgres'
POSTGRES_PASSWORD='pass123'
POSTGRES_DB='app'
POSTGRES_PORT='5432'
# Use 'db' when running inside devcontainer, 'localhost' for manual local setup
DB_HOST='db'

###################
# Redis Configuration
###################
# Use 'redis' when running inside devcontainer, 'localhost' for manual local setup
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=

###################
# JWT Configuration
###################
JWT_SECRET=your_secure_jwt_secret_here_minimum_32_characters
JWT_EXPIRATION=7d
JWT_REFRESH_SECRET=your_secure_jwt_refresh_secret_here_minimum_32_characters
JWT_REFRESH_EXPIRATION=30d

###################
# OTP Configuration
###################
OTP_EXPIRATION=300
OTP_LENGTH=4

###################
# SMS Configuration
###################
ASANAK_API='https://sms.asanak.ir/webservice/v2rest/sendsms'
ASANAK_API_USER='asanak_user_name'
ASANAK_API_PASS='asanak_api_password'
ASANAK_API_SOURCE='asanak_phone_password'

###################
# S3 Storage Configuration (RustFS)
###################
# Use 'rustfs' when running inside devcontainer, 'localhost' for manual local setup
S3_ENDPOINT=http://rustfs:9000
S3_DOMAIN=http://localhost/rustfs
S3_ACCESS_KEY=rustfsadmin
S3_SECRET_KEY=rustfsadmin
S3_BUCKET_NAME='app-dev'
S3_REGION=us-east-1

###################
# Payment Gateway Configuration
###################

# Zibal Payment Gateway
ZIBAL_MERCHANT=your_zibal_merchant_id
ZIBAL_CALLBACK_URL=http://app.ir/api/v1/payment/zibal-callback
ZIBAL_FRONTEND_CALLBACK_URL=http://monno.ir/dashboard/transaction/zibal
